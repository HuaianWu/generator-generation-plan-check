schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/generator-generation-plan-check"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh7dbfefb8b9a1cd991453104286acdb"
task_id = "generator-generation-plan-check"
display_title = "Add side-effect-free generation planning"
display_description = "Preview MyBatis Generator outputs through Java, CLI, and Maven interfaces without changing the target tree."
original_title = "为 MyBatis Generator 增加无副作用的代码生成 plan/check 模式"
category = "feature_request"
language = "java"
repository_url = "https://github.com/mybatis/generator"
base_commit_hash = "d4c81d303a723dee478b6ab0e73068bd93f48f34"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary d4c81d303a723dee478b6ab0e73068bd93f48f34 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh7dbfefb8b9a1cd991453104286acdb-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
