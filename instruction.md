请在仓库 https://github.com/mybatis/generator 的固定提交 d4c81d303a723dee478b6ab0e73068bd93f48f34（Tag：mybatis-generator-2.0.0）上，为 MyBatis Generator 增加一个可选的代码生成检查模式。

我们实际使用 MyBatis Generator 时，通常会把生成的 Java、Kotlin 和 XML 文件一起提交到 Git。数据库表结构、Generator 配置或模板发生变化后，目前只能重新执行一次生成，再通过 Git diff 判断哪些文件变了。这种做法在本地还可以接受，但放到 CI 中会直接修改工作区，而且很难区分真正的代码漂移、合并后的无变化、禁止覆盖时产生的 `.1` 文件，以及生成过程本身的错误。

希望增加一种无副作用的 plan/check 能力：完整执行现有的配置校验、数据库读取、插件处理、代码生成和格式化，按照正常生成时的规则计算最终会写入哪些文件以及文件内容，但检查过程不能真的修改目标目录。

功能要求：

1. 提供公开、可复用的 Java API，调用后可以取得本次生成计划。计划需要覆盖 Java、Kotlin、XML 和其他通用生成文件，并说明每个文件最终会写到哪里、属于新增、更新、无变化、另存还是跳过，以及对应的内容摘要和警告。
2. 检查模式必须使用正常生成时的实际规则。已有 Java 或 XML 文件需要合并时，应先算出合并后的内容再判断是否变化；允许覆盖时，也要区分内容真的变化和内容完全相同。禁止覆盖或无法合并时，如果正常生成会选择 `.1`、`.2` 等备用文件名，计划中应给出同一个最终路径。
3. 计划结果必须能准确预测紧接着执行正常生成时的结果。对计划中标记为新增、更新或另存的文件，实际生成后的路径和文件字节应与计划一致；已经相同的文件应标记为无变化，不要仅因为文件存在就判断需要更新。
4. 一次运行中可能有多个生成结果指向同一个目标路径。检查时不能只看运行前的磁盘状态，还要考虑本次计划中较早文件造成的结果。例如第一个文件计划占用了原路径后，后续文件是否合并、覆盖或改用 `.1`，应与正常保存顺序一致。
5. 检查过程不得创建目录、写入或删除文件、生成 `.1` 文件、留下临时文件，也不得调用项目刷新。取消操作仍应通过现有进度回调生效；可以报告读取数据库和生成代码的进度，但不能把计划中的文件报告成已经保存。
6. 为命令行增加适合 CI 使用的检查入口。没有代码漂移时进程成功；存在需要新增、更新或另存的文件时，应返回一个能够和普通执行错误区分的结果。命令行需要输出可读摘要，并支持将计划写成顺序稳定的 JSON 文件。
7. Maven Plugin 也应支持同样的可选检查能力。功能默认关闭；开启后，没有漂移时构建通过，发现漂移时构建失败并显示摘要，也可以输出 JSON 结果。Java API、命令行和 Maven Plugin 对同一输入应给出一致的文件状态、路径和摘要。
8. 内容摘要应基于文件按照实际编码得到的最终字节计算。同一配置和文件状态重复检查时，计划项顺序、最终路径、状态和摘要都应稳定，能够直接用于 CI 比较。
9. 增加自动化测试，使用仓库已有测试基础设施、临时目录和可用的内存数据库，覆盖新文件、已有文件无变化、覆盖更新、Java/XML 合并、禁止覆盖产生备用文件名、同次生成路径冲突、不同编码、取消操作和错误分支。测试还需要证明检查前后的目标目录完全一致，并验证检查结果与随后真实生成的结果相符。
10. 补充必要的使用说明和示例，说明 Java API、命令行和 Maven Plugin 的启用方式、各类文件状态、CI 中的结果判断、JSON 内容以及检查过程不会修改文件的保证。

边界与兼容性：

- 该能力默认关闭，未启用时现有生成、覆盖、合并、命令行和 Maven Plugin 行为保持不变。
- 保持当前固定版本使用的 Java 17、模块边界、公开 API 风格和测试风格。
- 不实现数据库迁移、DDL diff、Git 操作、远程检查服务、IDE 图形界面、模板编辑器或自动修复生成代码。
- JSON 不需要保存完整生成内容，只需包含可复现检查所需的文件类型、状态、最终路径、内容摘要和警告。
- 不规定具体类名、文件位置或内部实现方式，请按照仓库现有结构完成。
- 不要为这项能力增加新的第三方运行时依赖。
